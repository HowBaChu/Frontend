const LoginPage = () => {
    return (
        <div id="Wrapper">
            이 밖으로 나가지 않게 해주세요
            <div id="LoginFormContainer">
                눈에 보이는 모든 요소 감싸는 div
            </div>
        </div>
    );
};

export default LoginPage;